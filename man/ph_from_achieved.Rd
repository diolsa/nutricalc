% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ph.R
\name{ph_from_achieved}
\alias{ph_from_achieved}
\title{Compute pH of a nutrient solution from achieved composition}
\usage{
ph_from_achieved(
  achieved,
  temp_C = 25,
  phc_bracket = c(3, 9),
  tol = 1e-09,
  max_iter = 200,
  inner_max_iter = 50,
  debug = FALSE,
  chelate_z = c(EDTA = -4, DTPA = -5, EDDHA = -4, HBED = -4),
  gamma_model = 1
)
}
\arguments{
\item{achieved}{Named numeric vector of mmol/L (e.g., solver result `x$achieved`).
If present, `CO2_aq` is treated as dissolved free CO2(aq) (mmol/L), mapped
from BWB "Basenkapazitaet KB 8,2" / KS 8.2 and used to fix carbonate speciation.}

\item{temp_C}{Temperature in Celsius (only 25 C supported in this version).}

\item{phc_bracket}{Numeric length-2 bracket for pHc = -log10([H+]) search.}

\item{tol}{Root tolerance in meq/L.}

\item{max_iter}{Maximum iterations for the outer bisection.}

\item{inner_max_iter}{Maximum iterations for the ionic strength fixed point.}

\item{debug}{Logical; when TRUE prints basic bracket diagnostics.}

\item{chelate_z}{Named numeric vector of chelate charges to include as fixed
anions. Values are net charges (negative) for ligand totals in mmol/L.}

\item{gamma_model}{Integer 1 (Davies) or 2 (Debye–Hückel). Default 1.}
}
\value{
A list with pH, ionic strength, activity coefficients, species
  distribution, and charge balance diagnostics.
}
\description{
Uses charge balance with acid/base speciation and activity corrections
to estimate pH at 25 C. Assumes no carbonate alkalinity, no complexation, and
no precipitation.
}
\details{
gamma_model:
  1 = Davies (default)
  2 = Debye–Hückel limiting law (DH)
}
